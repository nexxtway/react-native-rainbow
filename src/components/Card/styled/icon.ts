import styled from 'styled-components/native';
import { MARGIN_SMALL } from '../../../styles/margins';

const StyledViewIcon = styled.View`
    flex-shrink: 0;
    margin-right: ${MARGIN_SMALL};
`;

export default StyledViewIcon;
