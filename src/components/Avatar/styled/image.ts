import styled from 'styled-components/native';

const StyledImage = styled.Image<{}>`
    position: absolute;
    border: none;
    border-radius: 24px;
    height: 100%;
    width: 100%;
`;

export default StyledImage;
